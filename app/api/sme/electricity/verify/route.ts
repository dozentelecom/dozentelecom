import {NextResponse} from 'next/server';import {smeapi,ProviderError} from '@/lib/smeapi';
export async function POST(req:Request){try{const b=await req.json();const provider=Number(b.provider),meternumber=String(b.meternumber||''),metertype=String(b.metertype||'');if(!Number.isFinite(provider)||!meternumber||!['prepaid','postpaid'].includes(metertype))return NextResponse.json({error:'provider, meternumber and metertype are required'},{status:400});return NextResponse.json(await smeapi.electricityVerify({provider,meternumber,metertype}))}catch(e:any){const s=e instanceof ProviderError?e.status:502;return NextResponse.json({error:e.message,details:e.details},{status:s})}}
