import {cookies} from 'next/headers';import {SignJWT,jwtVerify} from 'jose';const secret=()=>new TextEncoder().encode(process.env.AUTH_SECRET||'dev-secret');export async function setSession(id:string){const t=await new SignJWT({sub:id}).setProtectedHeader({alg:'HS256'}).setIssuedAt().setExpirationTime('7d').sign(secret());(await cookies()).set('dt_session',t,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:604800})}export async function currentUserId(){const t=(await cookies()).get('dt_session')?.value;if(!t)return null;try{return (await jwtVerify(t,secret())).payload.sub||null}catch{return null}}
