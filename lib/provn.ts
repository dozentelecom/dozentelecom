const base=(process.env.CLOUDEYE_BASE_URL||'https://api.cloudeye.ng').replace(/\/+$/,'');
function headers(){const access=process.env.PROVN_ACCESS_KEY||'',test=process.env.PROVN_TEST_KEY||'';return {'Content-Type':'application/json','Authorization':`Bearer ${access}`,'X-API-KEY':test,'API-Key':test,'Access-Key':access,'Accept':'application/json'};}
async function verify(path:string,body:Record<string,unknown>){const r=await fetch(`${base}${path}`,{method:'POST',headers:headers(),body:JSON.stringify(body),cache:'no-store'});const text=await r.text();let data:any;try{data=JSON.parse(text)}catch{data={raw:text}};if(!r.ok)throw new Error(data?.message||data?.error||`KYC provider error (${r.status})`);return data;}
export const verifyNin=(nin:string)=>verify('/verification/nin',{nin});export const verifyBvn=(bvn:string)=>verify('/verification/bvn',{bvn});
